# Templates

Use the minimum viable contract when you need a capability to be navigable with the smallest honest declaration.
Use the extended contract when the capability needs dependency, behavioral, and evolutionary context in the same artifact.
The worked examples show the fully populated `payment/retry` contract in both forms, alongside the audit and extraction-priority sheets that support the adoption journey.
