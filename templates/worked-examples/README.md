# Worked Examples

This directory contains the canonical `payment/retry` examples used across the toolkit, docs, and tests.
The examples are fully populated and should be treated as the reference shape for generated contract artifacts.
