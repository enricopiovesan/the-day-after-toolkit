# Agent Configurations

These files show how a contract-driven repository should instruct coding agents to navigate and validate their work. They are examples, not magic, so they should stay specific to the repo's commands, files, and validation rules.
